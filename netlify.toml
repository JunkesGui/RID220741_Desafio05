[build]
  base = "/"
  command = "npm run build"

  publish = "frontend/dist"

[functions]
  directory = "backend/build/src/shared/infra/functions"
  node_bundler = "nft"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200