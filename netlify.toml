[build]
  base = "/"
  command = "npm run build"

  publish = "frontend/dist"

[functions]
  directory = "backend/build/src/shared/infra/netlify.js"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200